diff --git a/CMakeLists.txt b/CMakeLists.txt\nindex 03ed01e..27851fc 100644\n--- a/CMakeLists.txt\n+++ b/CMakeLists.txt\n@@ -28,7 +28,7 @@ project(sherpa-onnx)\n # Remember to update\n # ./CHANGELOG.md\n # ./new-release.sh\n-set(SHERPA_ONNX_VERSION "1.13.8")\n+set(SHERPA_ONNX_VERSION "1.13.8+neko.kws2")\n\n # Disable warning about\n #\ndiff --git a/sherpa-onnx/csrc/keyword-spotter-transducer-impl.h b/sherpa-onnx/csrc/keyword-spotter-transducer-impl.h\nindex c8cb18d..171dca3 100644\n--- a/sherpa-onnx/csrc/keyword-spotter-transducer-impl.h\n+++ b/sherpa-onnx/csrc/keyword-spotter-transducer-impl.h\n@@ -191,7 +191,13 @@ class KeywordSpotterTransducerImpl : public KeywordSpotterImpl {\n     return s->GetNumProcessedFrames() + model_->ChunkSize() <\n            s->NumFramesReady();\n   }\n-  void Reset(OnlineStream *s) const override { InitOnlineStream(s); }\n+  void Reset(OnlineStream *s) const override {\n+    // Reset decoder hypotheses and encoder state, but preserve the output-frame\n+    // clock: the feature stream and processed input frames continue unchanged.\n+    int32_t frame_offset = s->GetKeywordResult().frame_offset;\n+    InitOnlineStream(s);\n+    s->GetKeywordResult().frame_offset = frame_offset;\n+  }\n\n   void DecodeStreams(OnlineStream **ss, int32_t n) const override {\n     for (int32_t i = 0; i < n; ++i) {\ndiff --git a/sherpa-onnx/csrc/version.cc b/sherpa-onnx/csrc/version.cc\n--- a/sherpa-onnx/csrc/version.cc\n+++ b/sherpa-onnx/csrc/version.cc\n@@ -20,7 +20,7 @@ const char *GetGitSha1() {\n }\n\n const char *GetVersionStr() {\n-  static const char *version = "1.13.8";\n+  static const char *version = "1.13.8+neko.kws2";\n   return version;\n }\n\n